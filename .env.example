# Salin file ini menjadi .env untuk menjalankan di komputer lokal.
# Ganti dengan connection string PostgreSQL milikmu (lokal atau dari Render).
DATABASE_URL=postgres://user:password@localhost:5432/warung_empuk
PORT=3000
